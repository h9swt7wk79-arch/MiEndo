{
  "name": "miendo",
  "version": "1.0.0",
  "description": "MiEndo — suivi de l'endométriose (app web + app iOS via Capacitor)",
  "private": true,
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "cap:sync": "npm run build && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/local-notifications": "^8.2.0"
  }
}
